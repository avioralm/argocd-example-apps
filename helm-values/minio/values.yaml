mode: distributed

auth:
  rootUser: "admin"
  rootPassword: "changeme-production-password"

statefulset:
  replicaCount: 4

persistence:
  enabled: true
  storageClass: "hostpath"
  size: 10Gi

resources:
  requests:
    memory: 512Mi
    cpu: 250m
  limits:
    memory: 2Gi
    cpu: 1000m

service:
  type: LoadBalancer
  port: 9000

consoleService:
  type: LoadBalancer
  port: 9001

ingress:
  enabled: false

metrics:
  serviceMonitor:
    enabled: false

provisioning:
  enabled: true
  buckets:
    - name: kafka
    - name: data
    - name: backups
  policies:
    - name: readonly
      statements:
        - resources:
            - "arn:aws:s3:::kafka/*"
          actions:
            - "s3:GetObject"
            - "s3:ListBucket"
    - name: readwrite
      statements:
        - resources:
            - "arn:aws:s3:::data/*"
          actions:
            - "s3:*"
  users:
    - username: kafka-user
      password: changeme-kafka-password
      disabled: false
      policies:
        - readwrite
        - consoleAdmin
      setPolicies: false
